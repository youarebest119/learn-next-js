import React from 'react'

export const metadata = {
    title: 'Custom Metadata',
    description: 'Wrriteten by custom user',
}

const page = () => {
    return (
        <div>
            Custom Metadata
        </div>
    )
}

export default page
